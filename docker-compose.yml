services:
  app:
    image: ${IMAGE_NAME:-registry.gitlab.com/dyuzha-group/devport}:${IMAGE_TAG:-latest}
    container_name: nuxt-site
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "${OUT_PORT:-3000}:3000"

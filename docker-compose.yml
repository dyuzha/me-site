services:
  app:
    build: .
    container_name: node_app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "80:80"
      - "3000:3000"
    networks:
      - app_network
